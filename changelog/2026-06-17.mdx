---
title: "June 17, 2026"
description: "Your task inbox sorts itself, a deeper API for builders, and a plain-language privacy page."
---

## Highlights

### Your task inbox sorts itself

Capture a task and ProductBrain files it for you. A quick thought becomes a memo, a small action becomes a do-it, and anything urgent gets flagged. Less triage, more capture.

### A deeper API for builders

If you drive ProductBrain from the terminal or your own agent, the v1 API is far more capable. It coaches the calling agent in-band with next-step hints and wrong-endpoint redirects, tells you what each call actually did, and gained full lifecycle control over share links and webhooks.

### Email and privacy, handled

One-click unsubscribe on every marketing email, an updated Privacy Policy and subprocessor list, and a new plain-language [Your data & privacy](/trust/your-data-privacy) page that lays out exactly how we handle your data — including what we deliberately don't yet claim.

## Other improvements

- Revoke or expire a public share link through the API
- Webhooks can now be updated and rotated in place, no delete-and-recreate
- Search and navigation responses describe themselves, so an agent always knows what it's looking at
